# Agentic-Document-organization-App
Agentic Document Organization` is a Python MVP that organizes messy document folders into a machine-generated folder tree. It ingests `.txt` and text-based `.pdf` files, stores normalized copies under its own managed workspace, generates embeddings with Gemini Embedding mdoels, clusters related files into recursive folders.
